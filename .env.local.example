# =============================================================================
# スカイツリー撮影プランナー v0.1.0 - ローカル環境設定
# =============================================================================
#
# Docker を使わないローカル環境での開発用設定
# cp .env.local.example .env.local
#
# =============================================================================

# 実行環境
NODE_ENV=development
PORT=8000

# =============================================================================
# データベース設定 (ローカル PostgreSQL)
# =============================================================================

# ローカル PostgreSQL 接続 (要インストール)
DATABASE_URL="postgresql://username:password@localhost:5432/skytree_photo_planner?schema=public"

# PostgreSQL 個別設定
DB_NAME=skytree_photo_planner
DB_USER=username
DB_PASSWORD=password

# =============================================================================
# Redis 設定 (ローカル Redis)
# =============================================================================

# ローカル Redis 接続 (要インストール)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_URL=redis://localhost:6379

# =============================================================================
# JWT 認証設定
# =============================================================================

# 開発用 JWT キー (ローカル環境のみ使用)
JWT_SECRET=local-dev-jwt-secret-key-not-for-production
REFRESH_SECRET=local-dev-refresh-secret-key-not-for-production

TOKEN_EXPIRY=1h
REFRESH_EXPIRY=7d

# =============================================================================
# ログ設定
# =============================================================================

# 開発用ログレベル
LOG_LEVEL=debug

# ファイルログは開発時は無効
ENABLE_FILE_LOGGING=false
LOG_DIR=./logs

# =============================================================================
# バックグラウンドワーカー設定
# =============================================================================

# ローカル環境での軽量設定
WORKER_CONCURRENCY=2
QUEUE_MAX_JOBS=50
QUEUE_CONCURRENCY=3

# =============================================================================
# 初期管理者アカウント設定
# =============================================================================

ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# =============================================================================
# フロントエンド設定
# =============================================================================

FRONTEND_URL=http://localhost:3001

# =============================================================================
# パフォーマンス調整
# =============================================================================

# ローカル環境での軽量設定
DB_POOL_MIN=1
DB_POOL_MAX=5

REDIS_MAX_RETRIES=3
REDIS_RETRY_DELAY=50

# 開発用緩いレート制限
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=200
RATE_LIMIT_ADMIN_MAX_REQUESTS=100
RATE_LIMIT_AUTH_MAX_REQUESTS=10
RATE_LIMIT_AUTH_WINDOW_MS=300000